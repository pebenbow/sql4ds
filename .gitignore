.Rproj.user
.Rhistory
.RData
.Ruserdata
.Renviron

/.quarto/

# Rendered book output. Both publish.yml (main) and draft-preview.yml
# (draft) render fresh in CI and deploy directly, so this is pure build
# output now -- nothing keeps a committed copy in sync with either branch.
/_book/

# Exercise solutions (not for distribution)
solutions/

# Dev symlink inside quarto-dbml extension (points back to source repo)
_extensions/quarto-dbml/node_modules/quarto-dbml

# Quarto render caches
*_cache/

# Claude Code project settings
.claude/

**/*.quarto_ipynb
**/*.quarto_ipynb_*
